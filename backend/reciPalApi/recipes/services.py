# Fill this later with your Gemini integration.
# Keep the signature so views don't change.
def generate_recipe_json_from_media(file_obj) -> dict:
    """
    Given an uploaded mp3/mp4 file-like object, return a dict with the parsed
    recipe JSON (e.g., {"transcript": "...", "ingredients": [...]}).
    Implement using Google GenAI (Gemini) Files API in your project.
    """
    raise NotImplementedError("Plug in Gemini transcription/parsing here.")
